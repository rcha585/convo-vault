# Convo Vault private vault template

This is an empty, public template. Run `knowledge:init` against a private destination to create the complete directory tree. Never place real personal or company data inside the repository template.
